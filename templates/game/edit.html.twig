{% extends 'base.html.twig' %}

{% block title %}Edit Game{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <link rel="stylesheet" href="{{ asset('styles/pages/admin.css') }}">
{% endblock %}

{% block body %}
    <h1>Modifier Jeux</h1>

    <div class="formEditContainer">
        {{ include('game/_form.html.twig', {'button_label': 'Update'}) }}
    </div>

    <div class="updateBtnContainer">
        <a class="button" href="{{ path('app_game_index') }}">back to list</a>

        {{ include('game/_delete_form.html.twig') }}
    </div>
{% endblock %} 
